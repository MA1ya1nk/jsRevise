// var can be redefined in a scope basically any value defined in a scope with var can also be use outside it's which is bad 
// you learned normal known things here

const addOne = function(num){  // imp another way of defining a function
    return num+1
}
 //addOne(5)  // [Function: addOne]
//console.log(addOne(5))  // 6

// if you define a function by this so you cannot use it before but in normal function you can use the function before defining it
