const obj1 = {1: "a", 2: "b",name: "mayank"};
const {name} = obj1;  // destructure this is similar to props in react
console.log(name);

const {name:nam} = obj1; // short name
console.log(nam);

// API return data in JSON(object) format
// JSON formater tool to read json data easily (convert it into tree

